@* @model IEnumerable<Imobi.Models.Identity.ApplicationUser>
@{
    ViewData["Title"] = "Usuários";
} *@

@* <div class="card-header d-flex justify-content-between align-items-center">
    <h5 class="mb-0">Gerenciamento de Usuários</h5>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i> Novo Usuário
    </a>
</div> *@
@* <h3 class="mb-4">Usuários</h3>
<p>
    <a asp-action="Create" class="btn btn-success">Novo</a>
</p>

<table class="table table-hover">
    <thead class="table-light">
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Status</th>
            <th>Roles</th>
            <th class="text-end">Ações</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var u in Model)
        {
            <tr>
                <td>@u.Nome @u.SobreNome</td>
                <td>@u.Email</td>
                <td>
                    @if (u.Ativo)
                    {
                        <span class="badge bg-success">Ativo</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Inativo</span>
                    }
                </td>
                <td>
                    <small class="text-muted">
                        (ver em "Editar Roles")
                    </small>
                </td>
                <td class="text-end">
                    <div class="gap-1 btn-group">
                        <a asp-action="Details" asp-route-id="@u.Id" class="btn btn-sm btn-outline-secondary" title="Detalhes">
                            <i class="bi bi-eye"></i>
                        </a>

                        <a asp-action="EditRoles" asp-route-id="@u.Id" class="btn btn-sm btn-outline-primary" title="Editar Roles">
                            <i class="bi bi-shield-lock"></i>
                        </a>

                        <a asp-action="ResetPassword" asp-route-id="@u.Id" class="btn btn-sm btn-outline-warning" title="Resetar Senha" onclick="return confirm('Redefinir senha deste usuário?');">
                            <i class="bi bi-key"></i>
                        </a>

                        <a asp-action="ToggleStatus" asp-route-id="@u.Id" class="btn btn-sm btn-outline-danger" title="Ativar/Desativar">
                            <i class="bi bi-power"></i>
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table> *@

@model List<UsuarioListItemViewModel>

<div class="container">
	<h3 class="mb-4">Gerenciar Usuários</h3>

	<p>
		<a asp-action="Create" class="btn btn-success">Novo</a>
	</p>
	<table class="table table-hover">
		<thead class="table-light">
			<tr>
				<th>Email</th>
				<th>Ativo</th>
				<th>Funções</th>
				<th>Ações</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var user in Model)
			{
				<tr>
					<td>@user.Email</td>
					<td>
						@if (user.Ativo)
						{
							<span class="badge bg-success">Ativo</span>
						}
						else
						{
							<span class="badge bg-danger">Inativo</span>
						}
					</td>

					<td>
						@foreach (var role in user.RolesNames)
						{
							<span class="badge bg-primary">@role</span>
						}
					</td>

					<td class="text-end">
                        <div class="d-flex gap-1">
							<a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-outline-primary" title="Editar">
                                <i class="bi bi-pencil-square"></i>
							</a>
							<a asp-action="Details" asp-route-id="@user.Id" class="btn btn-sm btn-outline-info" title="Detalhes">
								<i class="bi bi-eye"></i>
							</a>
                            <a asp-action="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-outline-danger" title="Excluir">
								<i class="bi bi-trash"></i>
							</a>

							<a asp-action="EditRoles" asp-route-id="@user.Id" class="btn btn-sm btn-outline-primary" title="Editar Roles">
                            <i class="bi bi-shield-lock"></i>
                        </a>

                        <a asp-action="ResetPassword" asp-route-id="@user.Id" class="btn btn-sm btn-outline-warning" title="Resetar Senha" onclick="return confirm('Redefinir senha deste usuário?');">
                            <i class="bi bi-key"></i>
                        </a>

                        <a asp-action="ToggleStatus" asp-route-id="@user.Id" class="btn btn-sm btn-outline-danger" title="Ativar/Desativar">
                            <i class="bi bi-power"></i>
                        </a>
						</div>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>